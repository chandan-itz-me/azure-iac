# AWS-0104 equivalent placeholder for Azure Trivy findings.
# Add specific check IDs here only after reviewing that the underlying risk is an accepted,
# documented exception (e.g. a control-plane resource that legitimately needs broad egress).
